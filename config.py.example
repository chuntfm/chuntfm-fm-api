import os

# Server Configuration  
HOST = os.getenv("HOST", "0.0.0.0")
PORT = int(os.getenv("PORT", "8000"))

# Webhook
WEBHOOK_KEYWORD = os.getenv("WEBHOOK_KEYWORD", "changeme")
WEBHOOK_START_KEY = os.getenv("WEBHOOK_START_KEY", "start")
WEBHOOK_STOP_KEY = os.getenv("WEBHOOK_STOP_KEY", "stop")

# Production Settings
DEBUG = os.getenv("DEBUG", "False").lower() == "true"
LOG_LEVEL = os.getenv("LOG_LEVEL", "INFO")

# API Configuration
API_TITLE = os.getenv("API_TITLE", "ChuntFM Radio API")
API_VERSION = os.getenv("API_VERSION", "0.1.0")
API_PREFIX = os.getenv("API_PREFIX", "/fm")

# FM Channel Configuration
SCHEDULE_API_NOW_ENDPOINT = os.getenv("SCHEDULE_API_NOW_ENDPOINT", "http://localhost:8000/schedule/now")
JUKEBOX_API_ENDPOINT = os.getenv("JUKEBOX_API_ENDPOINT", "http://localhost:9000/jukebox/now-playing")
RESTREAM_NOW_ENDPOINT = os.getenv("RESTREAM_NOW_ENDPOINT", "https://api.chunt.org/schedule/restream/now")

# FM Channels - configurable channel definitions
FM_CHANNELS = {
        1: {
            "id": 1,
            "name": "ChuntFM Main",
            "description": "Primary ChuntFM channel",
            "streams": [
                {
                    "url": "https://localhost:8080/stream128",
                    "format": "MP3",
                    "bitrate": 128,
                    "quality": "low"
                },
                {
                    "url": "https://localhost:8080/stream",
                    "format": "MP3",
                    "bitrate": 320,
                    "quality": "high"
                }
            ]
        },
        2: {
            "id": 2,
            "name": "ChuntFM Jukebox",
            "description": "!juke",
            "streams": [
                {
                    "url": "https://localhost:8080/stream2",
                    "format": "MP3",
                    "bitrate": 320,
                    "quality": "high"
                }
            ],
            "jukebox_mode": True
        }
    }